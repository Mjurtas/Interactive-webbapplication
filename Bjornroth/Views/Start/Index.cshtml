@{
    ViewData["Title"] = "Home Page";
}
@model Bjornroth.Models.ViewModels.MovieViewModel



<div class="start-search">
    <h2>SEARCH MOVIE</h2>
    @using (Html.BeginForm("Index", "SearchResults")) { 
        <input type="text" id="searchInput" name="searchInput" placeholder="Search movie title...." />
        <input type="submit" value="Search"/>
    }

    

    @*@foreach(var search in Model)
        {
            <p>@Html.DisplayFor(item => search.Title)</p>
        }*@
    <div class="search-result">
        @*Html.ViewData.Model.Poster*@
        <h2>@Html.ViewData.Model.Title</h2>
        <h2>@Html.DisplayNameFor(model => Model.NumberOfLikes);</h2>
        <h2>@Html.ViewData.Model.NumberOfLikes</h2>
        <h2>@Html.DisplayNameFor(model => Model.NumberOfDislikes);</h2>
        <h2>@Html.ViewData.Model.NumberOfDislikes</h2>
    </div>

</div>

